import { Logo } from "../icons";

export const MainHeader = () => {
  return (
    <header className="container sticky top-0 flex justify-center py-14 lg:justify-start">
      <Logo />
    </header>
  );
};
